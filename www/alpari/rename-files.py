import os

dirFS = '../plots/corrs/'

for filename in os.listdir(dirFS):
	# nameParts = filename.split('.png')
	# nameParts = nameParts[0].split(' - ')
	# newName = nameParts[1] + ' - ' + nameParts[2] + ' - ' + nameParts[0] + '.png'
	# os.rename(dirFS + filename, dirFS + newName)
	# print(newName)